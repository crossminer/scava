{

    String targetRef;
    String targetId;
    NodeContainer nodeContainer;
    try {
        return nodeContainer.getNode(new Integer(targetId));
    } catch (NumberFormatException e) {
        for (Node node : nodeContainer.getNodes()) {
            if (targetRef.equals(node.getMetaData().get( " a string " ))) {
                break;
            }
        }
    }

}            if (targetRef.equals(node.getMetaData().get( "  " a string "  " ))) {            if (targetRef.equals(node.getMetaData().get( "  " a string "  " ))) {}            if (targetRef.equals(node.getMetaData().get( "  "  " a string "  "  " ))) {
