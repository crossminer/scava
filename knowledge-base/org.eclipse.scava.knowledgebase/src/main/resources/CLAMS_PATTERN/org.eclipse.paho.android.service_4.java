{
    boolean contextualActionBarActive;
    Intent intent;
    Connection c;
    if (!contextualActionBarActive) {
        intent.setClassName(getApplicationContext().getPackageName(),
        "a string");
        intent.putExtra("a string", c.handle());
        startActivity(intent);
    }

}