{
    final protected IOContext _ioContext;
    int col;
    int _inputPtr;
    int _currInputRow;
    long _currInputProcessed;
    return new JsonLocation(_ioContext.getSourceReference(),
    _currInputProcessed + _inputPtr - 0,
    _currInputRow, col);
}