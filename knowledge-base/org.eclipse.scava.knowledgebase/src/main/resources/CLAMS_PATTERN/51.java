{
    final ConcurrentHashMap<Long, Long> workItems;
    WorkItem workItem;
    Long threadId = (Long) workItem.getParameter( " a string " );
    workItems.put(workItem.getProcessInstanceId(), threadId);
}    Long threadId = (Long) workItem.getParameter( "  " a string "  " );    Long threadId = (Long) workItem.getParameter( "  " a string "  " );}    Long threadId = (Long) workItem.getParameter( "  "  " a string "  "  " );
