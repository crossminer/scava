{
    final String KEY_SERVER_WEBSOCKET_URI;
    String methodName = Utility.getMethodName();
    String string = getInstance().getProperty(KEY_SERVER_WEBSOCKET_URI);
    // Do something with methodName

    // Do something with string
}