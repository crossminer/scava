version: '2'
services:
    scavaadministration-mongodb:
        image: mongo:3.4.10
        ports:
            - "27017:27017"
        command: mongos --configdb scavaadministration-mongodb-config
    scavaadministration-mongodb-node:
        build:
            context: .
            dockerfile: mongodb/MongoDB.Dockerfile
        command: mongod --replSet rs1 --noprealloc --smallfiles
    scavaadministration-mongodb-config:
        image: mongo:3.4.10
        container_name: scavaadministration-mongodb-config
        command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
